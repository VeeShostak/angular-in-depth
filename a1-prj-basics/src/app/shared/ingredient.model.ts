// using accessor public: behind the scenes it will create the properties and assign them to the passed args
export class Ingredient {
    constructor(public name: string, public amount: number) {}
  }
  